'use client';
import React from 'react';

export default function PainelExecutivoPage() {
  return (
    <div className="card animate-fade-in">
      <h2 style={{ color: 'var(--primary)', marginBottom: '1rem', fontWeight: 'bold' }}>Painel Executivo</h2>
      <p style={{ color: 'var(--muted)' }}>
        O resumo executivo para apresentação e geração de PDF estará disponível aqui na próxima etapa.
      </p>
    </div>
  );
}
