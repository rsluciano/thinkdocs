'use client';
import React from 'react';

export default function ListaMestraPage() {
  return (
    <div className="card animate-fade-in">
      <h2 style={{ color: 'var(--primary)', marginBottom: '1rem', fontWeight: 'bold' }}>Lista Mestra (Documentos)</h2>
      <p style={{ color: 'var(--muted)' }}>
        A integração com a documentação do sistema estará disponível aqui na próxima etapa.
      </p>
    </div>
  );
}
